import NavBar from "../Navbar/NavBar";
import classes from "./Home.module.css"
const Home = () => {
  return (
    <div className={classes.Home}>
    <NavBar/>
    </div>
  );
};

export default Home;
